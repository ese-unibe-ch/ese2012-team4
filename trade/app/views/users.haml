
%h2 All Users
%br
- content_for :name do
  All_users
%ul
  - for user in all_users
    %li
      %em= user.get_name
      %em= " has #{user.list_items.size} Items to sell"
      \:
      %a(href="/users/#{user.name}" ) Show Users Items

