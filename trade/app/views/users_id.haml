
%h2= "#{params[:id]}"
%br
%br #{user.name}'s interests: #{user.description}
%br
%h3= "#{user.name}'s items on sale:"
- content_for :name do
  #{user.name}'s Active_items
%ul
  - for item in active_items
    %li
      %em= item.name
      %em= item.price
      %em= item.description
      \:
      %a(href="/buy/#{item.id}" ) Buy this item
      -#TODO: refactor to POST!
